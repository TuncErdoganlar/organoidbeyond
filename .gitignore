node_modules
dist
dist-ssr
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env (never commit real keys)
.env
.env.local
.env.*.local

# Never commit compiled JS next to `api/*.ts` — Vercel treats both as `api/pubmed` and errors.
api/pubmed.js
api/pubmed.d.ts
# Same for root TS sources (tsc emit); keep only `.ts` sources.
vite.config.js
# Compiled siblings under `api/` (avoid Vercel path collisions + junk in git)
api/pubmedUpstream.js
api/pubmedUpstream.d.ts
